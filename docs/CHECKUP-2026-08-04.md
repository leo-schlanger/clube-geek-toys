# Checkup completo — 4 de Agosto de 2026

> Smoke ponta a ponta em produção (HTTPS) + auditoria de superfície de código.  
> **Sem SSH na VPS** (chave desta máquina não está em `authorized_keys`).  
> Fluxos autenticados (login real, Stripe live, admin CRUD com sessão) **não** exercitados.

**Commits no ar (clube):** `07f53f4` brand + `998c269` eventos/admin redirect — CI Deploy **success**.  
**Home (repo):** `6b326d8` polish + `e9e65c6` promo — **produção `geeketoys.com.br` ainda sem as novas promoções** (deploy do host do home pendente/atrasado).

---

## 1. Resumo executivo

| Área | Status produção | Notas |
| --- | --- | --- |
| API + Postgres | ✅ | `/health` ok nos dois espelhos |
| Club / Adm / Shop SPA | ✅ | 200 + assets (geeketoys + geekpoptoys) |
| Admin canônico | ✅ | `admin.*` → 301 → `adm.*` |
| CORS | ✅ | Origens legítimas liberadas; `evil.example.com` sem ACAO |
| Cert SAN (14 nomes) | ✅ | Inclui admin/adm/club/shop/api/radio/analytics × 2 raízes |
| Brand Hot Pink no SPA | ✅ | CSS prod com `340 85% 59%` e `#F04080` |
| Rádio now-playing | ✅ | JSON estático AzuraCast |
| Catálogo loja | ⚠️ | Só **2 produtos** / **1 categoria** (seed checkup) |
| Evento | ⚠️ | Feature no ar; **conteúdo placeholder** + **0 fotos** |
| Site institucional promo | ❌/⚠️ | HTML prod **não** contém links shop/club/assinar/evento novos |
| SSH VPS (agent) | ❌ | Sem acesso para logs/docker in-box |
| Login/checkout real | ⬜ | Não testado (precisa credenciais / cartão) |

**Veredito:** plataforma **operacional** para clube + admin + loja (infra/API/SPA). Faltam: **conteúdo real** (produtos/evento), **deploy do home**, e validação **autenticada** + **VPS shell**.

---

## 2. Smoke produção (resultados)

### 2.1 Health

- `GET https://api.geeketoys.com.br/health` → **200** `{ status: ok, database: connected }`
- `GET https://api.geekpoptoys.com.br/health` → **200** (mesmo backend)

### 2.2 Frontends

| Host | HTTP |
| --- | --- |
| club / adm / shop × geeketoys | 200 |
| club / adm / shop × geekpoptoys | 200 |
| geeketoys.com.br (institucional) | 200 |

Rotas SPA críticas (login, assinar, cadastro, recuperar-senha, termos, privacidade, adm/login, adm/admin, pdv, shop/, evento, carrinho, checkout, entrar, categoria) → **todas 200** (HTML do shell; auth client-side).

### 2.3 Admin redirect

- `admin.geeketoys.com.br` → **301** `https://adm.geeketoys.com.br/`
- `admin.geekpoptoys.com.br` → **301** `https://adm.geekpoptoys.com.br/`

### 2.4 API pública / proteção

| Endpoint | Resultado | Esperado? |
| --- | --- | --- |
| `GET /products` | 200, total=2 | ✅ (catálogo magro) |
| `GET /products/categories` | 200, 1 cat | ✅ |
| `GET /categories` | 404 | ✅ (path correto é sob `/products`) |
| `POST /auth/login {}` | 400 validação | ✅ |
| `GET /auth/me` sem token | 401 | ✅ |
| `GET /orders` sem auth | 401 | ✅ |
| `POST /webhook/stripe` | 400 missing signature | ✅ endpoint vivo (exige header Stripe) |

### 2.5 CORS

- shop / club / adm.geekpoptoys → `Access-Control-Allow-Origin` espelhado + credentials
- evil.example.com → **sem** allow-origin (rejeitado)

### 2.6 Rádio

- `GET …/nowplaying_static/geek_e_toys.json` → **200** (título/artista presentes)

### 2.7 Brand no bundle shop

- Token primary **`340 85% 59%`** e hex **`#F04080` / `#FCBE04`** presentes no CSS de produção
- Sem residual `#7c3aed` / `#d4a520` no CSS/JS principal amostrado

---

## 3. Mapa de funções (código × papel)

### 3.1 Front (um bundle, 3 modos por subdomínio)

| Modo | Host | Rotas principais |
| --- | --- | --- |
| **member** | `club.*` | `/assinar`, `/cadastro`, `/login`, `/membro`, pagamentos, termos |
| **admin** | `adm.*` (canônico) | `/login`, `/admin`, `/pdv` |
| **shop** | `shop.*` | `/`, `/categoria/:slug`, `/produto/:slug`, `/carrinho`, `/checkout`, `/pedido/:id`, `/entrar`, `/evento` |

### 3.2 API Express

| Prefixo | Função |
| --- | --- |
| `/health` | Health + DB |
| `/auth/*` | Register, login, Google, refresh, logout, me, email verify, password reset |
| `/members/*` | CRUD membro, lookup CPF (PDV), pagamentos/assinatura por membro |
| `/users/*` | Users admin |
| `/pix` `/checkout` `/payment` `/payments` | PIX + cartão |
| `/subscription/*` | Assinatura Stripe |
| `/webhook` | Stripe webhooks (HMAC) |
| `/email/*` | Envio / templates |
| `/contracts/*` | Contrato digital + PDF |
| `/reports/*` `/cron/*` | Relatórios + cron admin |
| `/logs/*` `/audit/*` | Logs e export audit |
| `/lgpd/*` | Export / delete account |
| `/settings/*` | Config admin |
| `/products/*` | Catálogo + categorias + upload imagens |
| `/orders/*` | Pedidos loja + confirm PIX |

### 3.3 Jobs (cron 06:00 UTC)

- Lembrete de renovação (5–8 dias)
- Expiração de membros

### 3.4 Eventos (Laura)

- Config: `src/data/event.ts` (+ espelho home)
- Loja: banner + `/evento` + reserva WhatsApp + galeria download
- Home: seções/event banner no **código**; **não refletido no HTML live** do apex

---

## 4. Gaps e riscos (priorizados)

### ALTO — impacta negócio / go-live de conteúdo

| # | Item | Detalhe | Ação |
| --- | --- | --- | --- |
| A1 | **Home sem promo nova** | Prod `geeketoys.com.br` não tem `shop.geeketoys` / `club.geeketoys` / Assinar / Evento no HTML/JS servido | Disparar **deploy do repo `geek-toys-home`** no host do apex |
| A2 | **Catálogo quase vazio** | 2 produtos checkup, 1 categoria | Cadastrar produtos reais no admin |
| A3 | **Evento placeholder** | `enabled: true` com data fictícia, `photos: []` | Laura preenche `event.ts` + sobe fotos nos 2 repos |
| A4 | **Confirmar URL Stripe webhook** | Endpoint correto: `POST /webhook/stripe` (400 sem signature = OK). Validar no Dashboard se aponta para `https://api.geeketoys.com.br/webhook/stripe` | Conferir Stripe + secret |

### MÉDIO — qualidade / ops

| # | Item | Detalhe |
| --- | --- | --- |
| M1 | PIX loja manual | Confirm admin; sem gateway PIX auto |
| M2 | Frete = 0 | Checkout sem frete real |
| M3 | Cobertura testes ~11% | Meta 70% no TODO |
| M4 | JWT ainda em localStorage | Cookie httpOnly parcial (refresh); access no client |
| M5 | Google OAuth | Docs locais: client “não configurado” no clube |
| M6 | Sincronizar `event.ts` home ↔ clube | Duas fontes de verdade |
| M7 | SSH agent → VPS | Sem chave nesta máquina — logs/docker não auditados in-box |

### BAIXO / FUTURO

- Storybook, push, multi-tenant, frete, Redis, monitoring, app mobile  
- Ingresso pago via Stripe + validação de membro  
- Limpar produto “Checkup” de teste do catálogo  

### Fora de escopo deste smoke (precisa sessão)

- Login admin/membro com senha real  
- Stripe PaymentIntent 3DS  
- Assinatura contrato + PDF e-mail  
- Upload imagem produto  
- PDV QR/CPF com membro real  

---

## 5. Checklist de “falta algo?” por fluxo

| Fluxo | Código | Produção smoke | Conteúdo / config |
| --- | --- | --- | --- |
| Cadastro + verify e-mail | ✅ | Shell 200; e-mail Resend depende env | — |
| Login membro/admin | ✅ | 401/400 sem credenciais | Credenciais reais |
| Assinatura anual + Stripe | ✅ | — | Webhook URL/secret |
| PIX assinatura | ✅ | — | Confirmação admin |
| Carteirinha + QR | ✅ | Brand card no código | — |
| PDV | ✅ | Shell 200 | Membro ativo real |
| Loja catálogo | ✅ | API 200 | **Produtos reais** |
| Carrinho + checkout | ✅ | Shell 200 | Frete/PIX |
| Evento + ingresso WA | ✅ | `/evento` 200 | **Dados/fotos reais** |
| Home → clube/loja/evento | ✅ código | **❌ live desatualizado** | Deploy home |
| Rádio player site | ✅ | Now-playing 200 | — |
| Espelho geekpoptoys | ✅ | DNS/cert/CORS ok | — |
| Admin `admin` vs `adm` | ✅ | 301 ok | Preferir `adm.*` |

---

## 6. Git / deploy no momento do checkup

| Repo | Branch | HEAD local/remoto | Observação |
| --- | --- | --- | --- |
| clube-geek-toys | master | `07f53f4` limpo | Deploy VPS success |
| geek-toys-home | main | `6b326d8` limpo | Apex ainda antigo |

---

## 7. Próximos passos recomendados (ordem)

1. **Deploy do site institucional** (`geek-toys-home`) para o host de `geeketoys.com.br`.  
2. Confirmar no Stripe Dashboard a **URL exata do webhook** e retestar com assinatura.  
3. Popular **catálogo** e **evento real** (textos + fotos).  
4. Colocar **pubkey desta máquina** na VPS → checkup docker/logs/nginx in-box.  
5. Rodar **E2E autenticado** (Playwright) com usuário de staging se existir.

---

## 8. Limitações deste checkup

- Sem SSH: não vi `docker compose ps`, logs da API, `.env` da VPS, disco, backups.  
- Sem credenciais: não validei painel logado nem pagamento live.  
- Home: strings buscadas no HTML/JS servido; se o host cachear agressivo, pode atrasar o novo build mesmo após deploy.

_Fim do checkup 2026-08-04._
