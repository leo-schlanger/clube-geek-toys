# Checkup completo — 10 de Agosto de 2026

> Smoke produção + revisão de fluxos + feature Atacado (código local, pendente deploy).

## 1. Resumo executivo

| Área                         | Status   | Notas                                                |
| ---------------------------- | -------- | ---------------------------------------------------- |
| API + Postgres               | ✅       | `/health` ok, DB connected                           |
| SPA shop / club / adm / home | ✅       | 200 nos hosts principais                             |
| Catálogo varejo              | ⚠️       | 15 produtos ativos; fotos ainda dependem de operação |
| Auth sem token               | ✅       | `/auth/me` → 401                                     |
| Orders sem body              | ✅       | POST `/orders` {} → 400 (validação ok)               |
| Canal Atacado (código)       | ✅ local | Schema + API + UI prontos; **não deployado** ainda   |
| Estruturas de dados          | ✅       | migration 012 alinhada a `ensureSchema`              |

**Veredito prod atual:** plataforma **operacional** nos fluxos existentes.  
**Atacado:** implementado no repo `D:\Projetos\clube-geek-toys`; entra em prod após deploy.

## 2. Smoke produção (2026-08-10)

| Check                                  | Resultado                                 |
| -------------------------------------- | ----------------------------------------- |
| `GET /health`                          | 200 `{ status: ok, database: connected }` |
| `GET /products?limit=3`                | 200, total **15**                         |
| `GET /products/categories`             | 200                                       |
| `GET /auth/me` (sem token)             | 401                                       |
| `POST /orders` `{}`                    | 400                                       |
| shop / club / adm / www                | 200                                       |
| shop `/entrar`, `/evento`, `/carrinho` | 200                                       |

## 3. Fluxos existentes (revisão)

| Fluxo                      | Estado         | Observação                                    |
| -------------------------- | -------------- | --------------------------------------------- |
| Vitrine loja               | OK             | Catálogo público                              |
| Carrinho localStorage      | OK             | Chave varejo separada da atacado (pós-deploy) |
| Checkout retail + frete    | OK no código   | Desconto `member_15` server-side              |
| Login loja / clube / admin | Superfície 200 | Credenciais E2E não exercitadas neste smoke   |
| Evento                     | OK             | `/evento` 200                                 |
| Admin produtos/pedidos     | OK no código   |                                               |
| Crédito loja / reviews     | Schema 010–011 |                                               |
| Home institucional         | OK             | Vercel                                        |

## 4. Estruturas de dados — Atacado (012)

| Objeto               | Campos novos                                                                |
| -------------------- | --------------------------------------------------------------------------- |
| `wholesale_accounts` | cnpj, company, status pending/approved/rejected/disabled, business_activity |
| `products`           | `wholesale_enabled` (default false), `wholesale_min_qty` CHECK ≥1           |
| `orders`             | `channel` retail\|wholesale, `customer_cnpj`, `wholesale_account_id` FK     |
| `config`             | `wholesale.enabled`, `wholesale.discount_percent` = 25                      |

Desconto atacado **não empilha** com membro. Frete sem desconto.

### Pendências de schema/código revistas (10/08)

| Item                                                           | Status          |
| -------------------------------------------------------------- | --------------- |
| CHECK `wholesale_min_qty` no ensureSchema                      | ✅ corrigido    |
| FK `orders.wholesale_account_id`                               | ✅ ensureSchema |
| LGPD export `wholesaleAccount`                                 | ✅              |
| LGPD delete redige CNPJ/empresa + zera `customer_cnpj`         | ✅              |
| Qty max checkout 999 (atacado)                                 | ✅              |
| PDP atacado só se `wholesale_enabled`                          | ✅              |
| Docs PROJECT / ARCH / SHOP / SECURITY / WHOLESALE / DOC-STATUS | ✅              |

## 5. O que falta operacionalmente

1. **Deploy** (push `master` → CI/VPS; ensureSchema no boot).
2. **Admin → Atacado:** aprovar CNPJs (objeto da compra).
3. **Produtos:** marcar “Disponível no atacado” na importação (só então vitrine enche).
4. Fotos dos produtos (pendência anterior Laura).
5. Token Melhor Envio (ops; frete fallback OK).

## 6. Como testar após deploy

1. `shop.../atacado` → estado “catálogo em preparação” se nenhum SKU liberado.
2. Cadastrar CNPJ válido → status pendente.
3. Admin aprovar → login com mesmo CNPJ → 25% no checkout.
4. Marcar 1 produto wholesale → aparece na aba.
5. LGPD export deve listar `wholesaleAccount` se existir.
